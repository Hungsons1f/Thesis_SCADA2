<UserControl x:Class="Thesis_SCADA.UserControls.ucWatchBoxRod"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Thesis_SCADA.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="15" d:DesignWidth="50"
             
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             TextElement.Foreground="{DynamicResource MaterialDesignBody}"
             TextElement.FontWeight="Regular"
             TextElement.FontSize="13"
             TextOptions.TextFormattingMode="Ideal"
             TextOptions.TextRenderingMode="Auto"
             FontFamily="{DynamicResource MaterialDesignFont}"
>
    <Grid>
        <Rectangle  Height="3" VerticalAlignment="Center" 
                    Fill="{DynamicResource PrimaryHueMidBrush}"/>
        <Rectangle  Height="15" Width="7" HorizontalAlignment="Left" VerticalAlignment="Center" 
                    Fill="{DynamicResource PrimaryHueMidBrush}"
                    Stroke="{DynamicResource MaterialDesignPaper}" StrokeThickness="1">
        </Rectangle>
    </Grid>
</UserControl>
